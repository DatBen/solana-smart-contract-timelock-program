[features]
seeds = false
skip-lint = true

[provider]
cluster = "http://localhost:8899"
wallet = "/home/datben/.config/solana/id.json"

[scripts]
test = "yarn run ts-mocha -p ./tsconfig.json -t 1000000 tests/**/timelock.ts"
